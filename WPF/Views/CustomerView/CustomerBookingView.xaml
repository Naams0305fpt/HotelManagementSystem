<UserControl x:Class="PhamHuynhSumWPF.Views.CustomerView.CustomerBookingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PhamHuynhSumWPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="{StaticResource LightBrush}">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Check-in Date:" Style="{StaticResource LabelStyle}"/>
            <DatePicker SelectedDate="{Binding StartDate}"/>

            <TextBlock Text="Check-out Date:" Style="{StaticResource LabelStyle}" Margin="20,5,0,0"/>
            <DatePicker SelectedDate="{Binding EndDate}"/>

            <Button Content="Search Availability" Command="{Binding SearchCommand}" Margin="20,5,5,5" Width="125"/>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding AvailableRooms}" SelectedItem="{Binding SelectedRoom}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Room Number" Binding="{Binding RoomNumber}" Width="*"/>
                <DataGridTextColumn Header="Room Type" Binding="{Binding RoomType.RoomTypeName}" Width="*"/>
                <DataGridTextColumn Header="Description" Binding="{Binding RoomDescription}" Width="2*"/>
                <DataGridTextColumn Header="Capacity" Binding="{Binding RoomMaxCapacity}" Width="Auto"/>
                <DataGridTextColumn Header="Price/Date" Binding="{Binding RoomPricePerDate, StringFormat='C'}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="2" Content="Book Selected Room" 
                Command="{Binding BookNowCommand}" 
                HorizontalAlignment="Right" Margin="0,10,0,0" 
                Width="150" Height="30"/>
    </Grid>
</UserControl>